conky.config = {
    alignment = 'bottom_right',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = '#eeeeee',
    double_buffer = true,
    font = 'Open Sans:size=35',
    gap_x = 0,
    gap_y = 0,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 190,
    own_window_type = 'desktop',
    own_window_hints = 'above,sticky',
    show_graph_range = false,
    update_interval = 1.0,
    use_spacer = 'none',
    use_xft = true,
}

conky.text = [[
${font}${exec cat time.txt|head -n 1}
${font Open Sans:size=20}${alignc}${exec cat time.txt|tail -1}
]]
